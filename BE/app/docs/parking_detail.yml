tags:
  - Parking
summary: 주차장 상세 조회
parameters:
  - name: id
    in: path
    required: true
    type: integer
    description: 조회할 주차장 ID
responses:
  200:
    description: 주차장 상세 정보 반환
    schema:
      type: object
      properties:
        status:
          type: string
          example: success
        data:
          type: object
          properties:
            parking_id:
              type: integer
            parking_name:
              type: string
            address:
              type: string
            price_per_hour:
              type: integer
            total_spots:
              type: integer

            # 🔥 자리 현황 추가
            available_spots:
              type: integer
              example: 12
            occupied_spots:
              type: integer
              example: 8

            distance_km:
              type: number
            ev_charge:
              type: boolean
              description: "주차장에 충전소가 있는지 여부"
              example: true
            layout:
              type: array
              items:
                type: object
                properties:
                  spot_id:
                    type: integer
                  status:
                    type: string
                  color:
                    type: string
                  ev_charge:
                    type: boolean
                    description: "충전소 여부"
            buttons:
              type: object
              properties:
                reserve:
                  type: boolean
                  example: true
                route:
                  type: boolean
                  example: true

  404:
    description: 주차장 없음
    schema:
      type: object
      properties:
        status:
          type: string
          example: fail
        message:
          type: string
          example: NOT FOUND
